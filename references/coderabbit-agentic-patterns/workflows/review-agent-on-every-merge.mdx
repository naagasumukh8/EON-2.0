---
title: Review-agent-on-every-merge
tagline: Every PR passes an automated review gate before merge, independent of who wrote the code.
attribution: Convergent pattern
tier: snippet
autonomous: false
canonical_url: https://agenticpatterns.io/the-review-gate
when_to_use: Any repo where agents write code that humans will merge.
when_not_to_use: Throwaway prototypes; codebases where human-only review is already deep and disciplined.
tags: [code-review, verification, gate, primitive]
use_cases: [code-review, building-features, dependency-upgrades, security-remediation, release-readiness]
inputs:
  - "Every PR opened against the protected branch"
  - "The repo's `AGENTS.md` so the reviewer knows the team's conventions"
  - "A configured review tool or review prompt (see /templates for examples)"
  - "Severity rules that say which findings block merge"
review_gate:
  trust: tool-assisted
  standards: tool-assisted
  merge: human-gate
  description: "Reviewer flags findings; human approves merge after the reviewer is satisfied."
checkpoints:
  - phase: pr-opened
    description: Automated review fires immediately; human waits for reviewer pass before reading the diff
  - phase: before-merge
    description: Human approval required even after automated review clears
sources:
  - title: The Review Gate (Agentic SDLC Handbook)
    url: https://agenticpatterns.io/the-review-gate
  - title: GitHub Blog, lessons from 2500+ AGENTS.md
    url: https://github.blog/ai-and-ml/github-copilot/how-to-write-a-great-agents-md-lessons-from-over-2500-repositories/
    year: 2025
related:
  patterns: []
  antiPatterns: [confidently-wrong-ai-slop, cookie-clicker-mode-passive-supervision]
  practices: [human-in-loop-checkpoints]
  glossary: [review-debt]
  tools: [coderabbit, greptile, sourcery, bito]
  workflows: [coderabbit-agent-mode, claude-code-github-action, plan-approval-gate]
draft: false
---

The pattern is one line of policy: **no PR merges without passing the review gate**. The gate's three questions (*Can we trust what changed? Does it meet our standards? Is it safe to merge?*) get answered by some combination of automated review, type-check, tests, and human reading. The composition varies by team; the policy doesn't.

The recipe is a GitHub branch-protection rule plus a webhook:

```text
Branch protection on `main`:
  ☑ Require pull request before merging
  ☑ Require status checks to pass:
      - review-agent (CodeRabbit / Greptile / etc.)
      - lint-and-typecheck
      - tests
  ☑ Require approvals (1+ human)
```

The pattern's wedge is the **independence** of the reviewer from the author. When a human writes the diff, a different human reviews. When an agent writes the diff, a different agent (or a human, or both) reviews. The reviewer never trusts the author's assertion that the change is correct. It verifies.

**Use for** any repo where agents will routinely write code. The cost is roughly one tool subscription and ~30 minutes of CI configuration; the payoff is the discharge mechanism for [review debt](/glossary/review-debt).

**Avoid for** throwaway prototypes and personal exploration repos. The gate is policy infrastructure; it shouldn't apply to playgrounds.

This is the universal pattern. Tool-specific implementations include [`coderabbit-agent-mode`](/workflows/coderabbit-agent-mode), [`claude-code-github-action`](/workflows/claude-code-github-action), and standalone review tools. See also [/the-review-gate](/the-review-gate) for the framework these all implement.
