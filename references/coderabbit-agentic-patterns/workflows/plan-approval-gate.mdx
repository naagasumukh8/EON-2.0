---
title: Plan-approval gate
tagline: Force the agent to write a plan first; you approve in plain English; only then does it write code.
attribution: Convergent pattern
tier: snippet
canonical_url: https://docs.claude.com/en/docs/claude-code/agent-teams
when_to_use: Anything non-trivial. Cheap discipline that prevents large unproductive code dumps.
when_not_to_use: Mechanical work where the plan is obvious, formatting, renames, automated tests.
tags: [plan-approval, gate, plan-act, primitive]
inputs:
  - "A non-trivial task description"
  - "The agent's read access to relevant files"
review_gate: {trust: human-only, standards: human-only, merge: plan-approval, description: "Human approves the plan before code is written; standard PR review applies to the resulting diff."}
checkpoints: [{phase: after-plan, description: "Human reviews and approves the plan before execution"}]
use_cases: [building-features, refactoring, migrations, spec-authoring, dependency-upgrades, security-remediation, documentation, release-readiness]
sources:
  - title: Agent Teams (Claude Code experimental)
    author: Anthropic
    url: https://docs.claude.com/en/docs/claude-code/agent-teams
    year: 2025
  - title: Plan & Act paradigm (Cline blog)
    author: Cline
    url: https://cline.bot/blog/plan-smarter-code-faster-clines-plan-act-is-the-paradigm-for-agentic-coding
    year: 2025
related:
  patterns: [plan-and-execute, plan-execute-verify-commit, ac-dc-cycle]
  antiPatterns: [plan-abandonment, one-shotting]
  practices: [use-plan-mode-for-multi-file-changes, human-in-loop-checkpoints]
  glossary: [plan-mode, plan-and-execute]
  workflows: [cline-plan-and-act, piv-loop]
---

The cheapest discipline that prevents large unproductive code dumps. Before the agent writes any code, it produces a written plan: scope, approach, files to touch, success criteria. The user approves in plain English. Only then does the agent execute.

The recipe is a single instruction prepended to the task prompt:

```text
First message rule: output PLAN ONLY. List scope, approach, files,
success criteria. Do NOT write code until I reply with "APPROVED".
```

That's it. The technique converges across multiple frameworks under different names, **Plan & Act** in Cline, **Plan mode** in Claude Code (`shift+tab`), **architect mode** in Aider, **Specify → Clarify → Plan** in Spec Kit. They all share the same beat: separate the *what* from the *how*, gate the second on a human OK.

The plan-first discipline does three things at once. It surfaces misunderstandings before they cost code. It gives you something concrete to push back on ("don't touch the auth module"). And it gives the agent something concrete to follow when it later loses thread, re-reading its own plan beats re-deriving it.

**Use for** anything non-trivial. The plan step costs ~1-2 minutes of conversation and saves an order of magnitude more by preventing rework.

**Avoid for** mechanical or trivially-bounded tasks: formatting passes, automated test stubs, dependency upgrades. The plan there is the task name, and the gate adds friction with no payoff.
